from rl_data_utils.utils.item.serie.is_functions import *


def has_accelerator_series(series: list[str]) -> bool:
    return any(map(is_accelerator_series, series))


def has_beach_blast_series(series: list[str]) -> bool:
    return any(map(is_beach_blast_series, series))


def has_bonus_gift(series: list[str]) -> bool:
    return any(map(is_bonus_gift, series))


def has_champions_1_series(series: list[str]) -> bool:
    return any(map(is_champions_1_series, series))


def has_champions_2_series(series: list[str]) -> bool:
    return any(map(is_champions_2_series, series))


def has_champions_3_series(series: list[str]) -> bool:
    return any(map(is_champions_3_series, series))


def has_champions_4_series(series: list[str]) -> bool:
    return any(map(is_champions_4_series, series))


def has_elevation_series(series: list[str]) -> bool:
    return any(map(is_elevation_series, series))


def has_ferocity_series(series: list[str]) -> bool:
    return any(map(is_ferocity_series, series))


def has_golden_egg_2018(series: list[str]) -> bool:
    return any(map(is_golden_egg_2018, series))


def has_golden_egg_2019(series: list[str]) -> bool:
    return any(map(is_golden_egg_2019, series))


def has_golden_egg_2020(series: list[str]) -> bool:
    return any(map(is_golden_egg_2020, series))


def has_golden_gift_2018(series: list[str]) -> bool:
    return any(map(is_golden_gift_2018, series))


def has_golden_gift_2019(series: list[str]) -> bool:
    return any(map(is_golden_gift_2019, series))


def has_golden_gift_2020(series: list[str]) -> bool:
    return any(map(is_golden_gift_2020, series))


def has_golden_lantern_2020(series: list[str]) -> bool:
    return any(map(is_golden_lantern_2020, series))


def has_golden_lantern_2021(series: list[str]) -> bool:
    return any(map(is_golden_lantern_2021, series))


def has_golden_pumpkin_2018(series: list[str]) -> bool:
    return any(map(is_golden_pumpkin_2018, series))


def has_golden_pumpkin_2019(series: list[str]) -> bool:
    return any(map(is_golden_pumpkin_2019, series))


def has_golden_pumpkin_2020(series: list[str]) -> bool:
    return any(map(is_golden_pumpkin_2020, series))


def has_haunted_hallows_series(series: list[str]) -> bool:
    return any(map(is_haunted_hallows_series, series))


def has_impact_series(series: list[str]) -> bool:
    return any(map(is_impact_series, series))


def has_nitro_series(series: list[str]) -> bool:
    return any(map(is_nitro_series, series))


def has_overdrive_series(series: list[str]) -> bool:
    return any(map(is_overdrive_series, series))


def has_players_choice_series(series: list[str]) -> bool:
    return any(map(is_players_choice_series, series))


def has_secret_santa_series(series: list[str]) -> bool:
    return any(map(is_secret_santa_series, series))


def has_spring_fever_series(series: list[str]) -> bool:
    return any(map(is_spring_fever_series, series))


def has_totally_awesome_series(series: list[str]) -> bool:
    return any(map(is_totally_awesome_series, series))


def has_triumph_series(series: list[str]) -> bool:
    return any(map(is_triumph_series, series))


def has_turbo_series(series: list[str]) -> bool:
    return any(map(is_turbo_series, series))


def has_velocity_series(series: list[str]) -> bool:
    return any(map(is_velocity_series, series))


def has_victory_series(series: list[str]) -> bool:
    return any(map(is_victory_series, series))


def has_vindicator_series(series: list[str]) -> bool:
    return any(map(is_vindicator_series, series))


def has_zephyr_series(series: list[str]) -> bool:
    return any(map(is_zephyr_series, series))


def has_zephyr_series(series: list[str]) -> bool:
    return any(map(is_zephyr_series, series))


def has_non_crate(series: list[str]) -> bool:
    return any(map(is_non_crate, series))
